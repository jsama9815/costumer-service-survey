<!-- for dev -->
1. npm run dev 
2. docker-compose up -d postgres
<!-- for database visual monitor -->
  docker compose up -d pgadmin

<!-- for prod -->
1. npm start
2. docker-compose up -d postgres
<!-- for database visual monitor -->
  docker compose up -d pgadmin